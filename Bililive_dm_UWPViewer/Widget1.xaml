<Page
    x:Class="Bililive_dm_UWPViewer.Widget1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Bililive_dm_UWPViewer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" d:DesignWidth="290" d:DesignHeight="464">

    <Grid>
        <ListView x:Name="ListView"  d:DataContext="{d:DesignInstance local:Model}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <local:Comment DataContext="{Binding}"></local:Comment>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsStackPanel ItemsUpdatingScrollMode="KeepLastItemInView" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>
       
    </Grid>
</Page>
